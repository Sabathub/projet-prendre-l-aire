// --- action types
import {
  
} from '../actions';
// --- initial state
const initialState = {
  file: {},
};
// --- Reducer
const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default: return state;
  }
};
// --- export
export default reducer;
